# Backlog Drain Prompt

```text
目标：把指定积压任务清到接近 0，并且避免重试风暴。

请按下面结构输出并执行：
1. 当前积压规模（SQL/命令）
2. 并发策略（并发数、批大小、间隔）
3. 退出条件（清空阈值、最大轮数、失败率阈值）
4. 失败分类（可重试/不可重试）
5. 断点续跑方案
6. 运行中观测项（吞吐、失败率、剩余量）
7. 收尾校验（剩余量、抽样质量）

硬约束：
- 先小流量验证，再开并发
- 出现高失败率立即降并发
- 避免无限循环
- 给出可复制的命令
```
